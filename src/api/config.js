export const SUCC_CODE = 0;
export const TIMEOUT = 100000;
export const RECOMMENDS_PERSIZE = 20;
export const JSONP_OPTIONS = {
  param: 'callback',
  timeout: TIMEOUT
};
export const jsonpOptions = {
  param: 'callback',
  timeout: TIMEOUT
};
